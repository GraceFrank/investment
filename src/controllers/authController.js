export const login = (req, res) => res.status(200).send(true);

export const register = (req, res) => {
  // validat request

  // check if user with email or phone already exists

  // create new user
  res.status(200).send(true);
};
